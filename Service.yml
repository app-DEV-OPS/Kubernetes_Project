kind: Service                             
apiVersion: v1
metadata:
  name: mrdevops
  labels:
    app: mrdevops
spec:
  ports:
    - port: 8086                               
      targetPort: 80                    
      nodePort: 31206
  selector:
    app: mrdevops                    
  type: LoadBalancer                   
