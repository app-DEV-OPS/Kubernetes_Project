kind: Service                             
apiVersion: v1
metadata:
  name: mrdevops
  labels:
    app: mrdevops
spec:
  ports:
    - port: 8085                               
      targetPort: 80                    
      nodePort: 31205
  selector:
    app: mrdevops                    
  type: LoadBalancer                   
