kind: Service                             
apiVersion: v1
metadata:
  name: mrdevops
  labels:
    app: mrdevops
spec:
  ports:
    - port: 8083                              
      targetPort: 80                    
      nodePort: 31203
  selector:
    app: mrdevops                    
  type: LoadBalancer                   
