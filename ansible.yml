- hosts:
    - 10.0.0.7
  become: true
  tasks: 
     - name: create new deployment
       shell: kubectl apply -f /home/ec2-user/Deployment.yml
     - name: create new service
       shell: kubectl apply -f /home/ec2-user/Service.yml -- sh -c
