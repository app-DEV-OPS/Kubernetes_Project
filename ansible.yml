- hosts: all
  become: true
  tasks: 
     - name: create new deployment
       command: envsubst < /home/ec2-user/Deployment.yml | kubectl apply -f -
     - name: create new service
       command: kubectl apply -f /home/ec2-user/Service.yml -- sh -c
